// (Full file — server API that hashes password, upserts user (Supabase if configured or demo fallback),
// issues a JWT as an HttpOnly cookie and returns { ok: true, redirect: '/dashboard' }.
// Paste the full contents I previously gave you for pages/api/set-password.js.)
