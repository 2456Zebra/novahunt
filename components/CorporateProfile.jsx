import CompanyProfile from './CompanyProfile';

// Compatibility wrapper: re-export CompanyProfile so existing imports of
// "CorporateProfile" keep working until we refactor all imports.
export default CompanyProfile;
